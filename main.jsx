import React from 'react';
import ReactDOM from 'react-dom/client';
import EmailClient from './EmailClient.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EmailClient />
  </React.StrictMode>
);